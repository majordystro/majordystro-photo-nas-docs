# diagrams/

This folder is reserved for architecture diagrams (PNG/SVG) and network topology images.

Planned contents:
- network-topology.svg — home network diagram (router, NAS, switch, clients, VPN)
- storage-layout.png — physical drive and volume layout diagram
- workflow-capture-one.svg — flow chart for tether → ingest → edit → export → backup
